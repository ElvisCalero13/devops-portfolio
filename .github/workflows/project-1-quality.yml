name: project-1-quality-checks

on:
  pull_request:
    paths:
      - "project-1-cicd/**"
      - ".github/workflows/project-1-quality.yml"
  # push:
  #   branches:
  #     - main
  #   paths:
  #     - "project-1-cicd/**"
  workflow_dispatch:

permissions:
  contents: read
  pull-requests: read

jobs:
  sonar:
    name: SonarQube analysis
    runs-on: ubuntu-latest

    defaults:
      run:
        working-directory: project-1-cicd

    steps:
      - name: Checkout repository
        uses: actions/checkout@v4
        with:
          fetch-depth: 0

      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: "3.11"

      - name: Install dependencies
        run: |
          python -m pip install --upgrade pip
          pip install -r app/requirements.txt

      - name: Run tests with coverage
        env:
          PYTHONPATH: app
        run: |
          pytest app/test_main.py --cov=app --cov-report=xml:coverage.xml

      - name: SonarQube Scan
        uses: SonarSource/sonarqube-scan-action@v6
        with:
          projectBaseDir: project-1-cicd
          args: >
            -Dsonar.projectKey=${{ vars.SONAR_PROJECT_KEY }}
            -Dsonar.organization=${{ vars.SONAR_ORGANIZATION }}
            -Dsonar.sources=app
        env:
          SONAR_TOKEN: ${{ secrets.SONAR_TOKEN }}
